APP_NAME=income_expense_personna
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://income-expense-personna.onrender.com

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

# Logging (optimisé pour Render)
LOG_CHANNEL=stderr
LOG_LEVEL=debug

# PostgreSQL Database (Render compatible)
DB_CONNECTION=pgsql
DB_HOST=
DB_PORT=5432
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=

# Session & Cache (optimisé pour production)
SESSION_DRIVER=database
SESSION_LIFETIME=120
CACHE_DRIVER=database
QUEUE_CONNECTION=database

# Redis (optional - comment if not used)
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# Email Configuration (Gmail example)
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@cheteuganial@gmail.com"
MAIL_FROM_NAME="${APP_NAME}"

# Vite Frontend Configuration
VITE_APP_NAME="${APP_NAME}"
VITE_APP_URL="${APP_URL}"